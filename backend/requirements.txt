# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
alembic>=1.13.1

# TRELLIS.2 is now the 3D generation backend (replaces Hunyuan3D).
# Install trellis2 from GitHub: pip install git+https://github.com/microsoft/TRELLIS.git
# Install o_voxel from GitHub: pip install git+https://github.com/microsoft/TRELLIS.git#subdirectory=extensions/voxel
trellis2
o_voxel

# Core ML dependencies (required by TRELLIS.2)
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0

# Mesh processing
trimesh>=4.0.0
pygltflib>=1.16.0
numpy>=1.24.0
fast_simplification>=0.1.10  # Required for mesh decimation

# Image processing
pillow>=10.0.0
rembg>=2.0.50
onnxruntime>=1.16.0
matplotlib>=3.8.0  # For headless thumbnail generation

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1
tqdm>=4.65.0
psutil>=5.9.0
